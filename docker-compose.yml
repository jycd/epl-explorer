version: "3.8"
services:
  epl-explorer:
    build: .
    ports:
      - "5001:5001"
    environment:
      - PORT=5001
    command: ["python3", "src/app.py"]
    volumes:
      - ./data:/app/data:ro
    restart: unless-stopped
